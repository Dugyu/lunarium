{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["@dugyu/luna-stage#build", "@dugyu/luna-core#build"],
      "inputs": [
        "src/**",
        "public/**",
        "rsbuild.config.ts"
      ],
      "outputs": ["dist/**"]
    }
  }
}
